name: Update Sentences Data

on:
  schedule:
    # 每天凌晨 3 点运行 (UTC)
    - cron: '0 3 * * *'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        
    - name: Fetch latest sentences
      run: node scripts/fetch-sentences.js
      
    - name: Build data module
      run: node scripts/build-data-module.js
      
    - name: Check for changes
      id: check_changes
      run: |
        if git diff --quiet data/ edge-functions/api/sentences-data.js; then
          echo "changed=false" >> $GITHUB_OUTPUT
        else
          echo "changed=true" >> $GITHUB_OUTPUT
        fi
        
    - name: Commit and push changes
      if: steps.check_changes.outputs.changed == 'true'
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add data/ edge-functions/api/sentences-data.js
        git commit -m "chore: auto-update sentences data $(date +%Y-%m-%d)"
        git push
